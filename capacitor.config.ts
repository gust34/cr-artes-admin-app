import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cr-artes-admin.portal',
  appName: 'cr-artes-admin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
